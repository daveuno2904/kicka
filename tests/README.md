# Tests

Validation tests for the KICKA schema and sample data.

This directory is a placeholder for v0.1. Planned contents:

- Schema validation tests that check every file in `data/samples/` and
  `docs/standard/*/examples/` against its corresponding
  `schemas/*/event.schema.json`
- Regression tests for the pipeline in [`pipeline/`](../pipeline)
